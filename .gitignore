# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — jamais commité
.env
.env*.local

# prisma
/prisma/*.db

# uploads : commités (vraies photos des annonces importées, nécessaires en
# prod tant que l'upload admin ne passe pas par un stockage objet externe).

# typescript
*.tsbuildinfo

# note de reprise de session (contient les identifiants admin de seed —
# usage local uniquement, jamais dans l'historique git)
/REPRISE-SESSION.md
